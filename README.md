# scoop-bucket (deprecated)

**This bucket is no longer updated.** `testbot-agent` moved to npm distribution — install runs
directly through your own Node.js (better antivirus/EDR compatibility on Windows than the bundled
runtime this bucket used to install). `bucket/testbot-agent.json` is frozen at its last published
version and will not receive further updates.

```sh
npm install -g testbot-agent
```

or

```sh
npx testbot-agent start
```

See the [testbot-agent README](https://github.com/testbots-ai/testbot-agent) for usage.
